import { SxProps, Theme } from '@mui/material';

export const STYLE = {
  CONTAINER: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 1,
  } as SxProps<Theme>,
} as const; 